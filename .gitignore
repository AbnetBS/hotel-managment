node_modules/
dist/
.env
.DS_Store
*.log

# Local database and staff uploads (runtime data, not source)
server/data/
server/uploads/*
!server/uploads/.gitkeep
# Guest identity scans — sensitive runtime data, never in source control
server/id-docs/
